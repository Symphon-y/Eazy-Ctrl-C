import React, { useContext } from 'react';

const Copy = () => {
  console.log(Object.keys(localStorage));
  return (
    <div>
      <h1>This is a Copy Page</h1>
    </div>
  );
};

export default Copy;
